# V13 Restructuring Audit Archive

This directory keeps historical audit notes from the v13 restructuring stage. These files are retained for traceability only; the current reviewer-facing entry points are `README.md`, `ARTIFACT_INDEX.md`, `data_dictionary.md`, and `reproduce_tables.py`.

The current manuscript-facing synthesis still uses some `data/v13_*` filenames because those CSVs became stable inputs during the prior restructuring stage. Those data files remain in `data/` and are validated by `reproduce_tables.py`.
